---
description: 项目全局约定、双认证体系与沟通语言
alwaysApply: true
---

# 项目核心

## 技术栈

- Laravel 11 风格：`bootstrap/app.php` 注册路由；`routes/web.php` 为前台；后台在 `routes/admin.php` 且 **URL 前缀 `/admin`、路由名前缀 `admin.*`**（见 `bootstrap/app.php` 的 `then` 闭包）。
- 前端资源：`@vite(['resources/css/app.css', 'resources/js/app.js'])`；无构建产物时 `master` 回退 CDN Tailwind + Alpine。

## 认证

- **前台会员**：`guard: web`，模型 `App\Models\User`，路由名 `front.*`；部分接口需 `middleware('auth')` 与 `front.active`（`EnsureFrontUserNotDisabled`）。
- **后台管理员**：`guard: admin`，模型 `App\Models\Admin\AdminUser`，路由名 `admin.*`；鉴权中间件 `admin.auth`（`AdminAuth`）。

## 配置

- `config/admin.php`：`name`、`upload_path`、`per_page`、`cache_time` 等。
- `config/front.php`：首页文章数、分页、缓存 TTL、SEO、表情包相关等。

## 协作约定

- **对话语言**：与用户沟通、解释与说明一律使用**简体中文**；不要默认使用繁体中文（除非用户明确要求繁体）。代码与路由名保持现有语言习惯（多为英文/拼音路由名 + 中文界面文案）。
- 改动聚焦需求：避免无关重构、不擅自新增说明文档（用户明确要求除外）。
- 可复现问题自行用终端验证（`php artisan route:list`、`migrate`、`test` 等），少让用户代跑命令。
-所有代码编写，都要加上简体中文注释。让研发能清晰理解代码实现的功能项。
