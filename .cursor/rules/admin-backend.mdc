---
description: 后台 Admin 控制器、请求类、视图与业务习惯
globs: app/Http/Controllers/Admin/**/*.php,app/Http/Requests/Admin/**/*.php,resources/views/admin/**/*.blade.php,routes/admin.php
alwaysApply: false
---

# 后台（Admin）

## 目录与分层

- 控制器：`App\Http\Controllers\Admin\*`。
- 表单验证：`App\Http\Requests\Admin\*`（如 `UserRequest`），在控制器中类型提示注入。
- 视图：`resources/views/admin/`，布局 `admin.layouts.master`。

## 路由与功能模块（对照 `routes/admin.php`）

- 仪表盘、个人中心（资料/密码/登录日志）。
- 前台会员 `members`：禁言/解禁/批量（勿与后台 `users` 混淆）。
- 后台账号 `users`：resource + `batch` + `export`。
- `roles`、`categories`（自定义路由绑定 id）、`articles`（含批处理、上传图、审核）、`comments`（无 create/store）、`settings`、`logs`（操作/错误）、`backups`、`menu-items`。

## 业务习惯

- 重要操作可记 `AdminOperationLog`（若模块已有该模式则保持一致）。
- 侧栏由 `admin_menu_items` 驱动；新页需在「菜单管理」或迁移/seeder 中增加项并填 `route_name`（`admin.*`）。
- 分页等优先 `config('admin.per_page', 10)` 或与现有列表页一致。

## 界面与主题

- `data-admin-theme`、`data-admin-sidebar` 在 **`<html>`**；侧栏十色块等关键样式用 **内联 `<style>` 中的纯 CSS 类**，避免依赖未编译的 Tailwind 任意类（详见 skill `admin-theme-layout`）。
